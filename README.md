task:
We need an app to quickly visualize logger readouts in the field.

This does NOT affect the WATINA import of logger files.

This necessity comes from a version update on the VuSitu side of things, who switched from `csv` to (zipped?) `html` files.

